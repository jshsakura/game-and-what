# Python
__pycache__/
*.pyc
.venv/
venv/

# Runtime data dir — library, db (+backups), recovery, tmp, name_map (never committed)
backend/data/

# Node / Vite
node_modules/
frontend/dist/
*.log

# Env
.env
.env.local

# Scratch artifacts — test runner output + one-off screenshots (root only)
.playwright-mcp/
/miyoo-ui.png

# scratch reference uploads
_refs/*
!_refs/README.md

# Local dev tooling / WIP with absolute paths — not part of the public release
backend/build_smw_kr.sh
backend/kr_msg_exgfx.patch
I18N_HANDOFF.md

# Local session handoff notes — not for the repo
HANDOFF.md

# ROM-translation tooling (kept out of the public repo; lives in the private tools repo)
backend/tools/

# teammp Korean-patch staging (patch files + adhoc applied roms) — personal use only
/korean-patches/

# Personal Korean-patch pipeline scripts (teammp scraper/matcher etc.) — local
# only, like backend/tools/: patch staging is personal-use, not for the repo
backend/scripts/teammp_*.py
backend/scripts/detect_prepatched.py
backend/scripts/manual_publish.py
backend/scripts/onion_sync.py
backend/scripts/pi_crc_scan.py
