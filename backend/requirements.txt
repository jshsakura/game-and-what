# PINNED, not floating. These were `>=` until 2026-07-27, which meant the same commit
# built a different image depending on the day: a rebuild months from now would pull
# whatever FastAPI had become, and the multi-arch job builds amd64 and arm64 on two
# separate runners, so a release landing between them could even put different versions
# in the two halves of one manifest.
#
# The versions below are what the published v1.11.3 image actually shipped, read out of
# the running container — so pinning changed nothing about what runs today, it only
# stopped it from drifting.
#
# To move one: bump it here deliberately, let CI's test job speak, and commit the bump
# as its own change. Do not widen these back to `>=` to "get fixes" — that is the
# behaviour this replaced.
fastapi==0.140.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pillow==12.3.0
httpx==0.28.1
py7zr==1.1.3

capstone==5.0.7
