# GBA — 실측 현황 (임시 메모, 지워도 됨)

> 이전 판(154코드, DB 병합만 한 것)은 **버렸습니다.** 그건 검증 안 된 주소였고,
> 실제로 돌려보니 그중 여럿이 틀렸습니다. 아래는 전부 **게임을 실행해서 잰 것**입니다.

정본은 `scripts/gba_idle_loop_db.json`, 펌웨어용은 `firmware/gba/gba_idle_loop.c`,
방법론은 `scripts/idlefind/README.md`, 실기 인수인계는 `docs/GBA_FIRMWARE_HANDOFF.md`.

## 현황

| | |
|---|---|
| 측정된 게임코드 | **515** (업로드하면 즉시 매칭) |
| ├ 아이들 주소 확보 (A/B 통과) | **89** |
| ├ 루프 불필요 — BIOS halt로 이미 쉼 | 329 |
| └ **루프 못 찾음 = 판정 불가** | **97** ← 다음 할 일 |
| 그중 진짜 예산 초과 (주소 있는데도) | **4** |

## 진짜 무거운 게임 (주소를 줬는데도 예산 초과)

| 게임 | 코드 | 부하 | 사이클 |
|---|---|---|---|
| 슈퍼 동키콩3 (Super Donkey Kong 3) | `BDQJ` | 112% | 178,881 |
| 동키콩 컨트리 3 (Donkey Kong Country 3) | `BDQE` | 112% | 178,575 |
| 슈퍼마리오 요시아일랜드 (Super Mario Advance 3 - Yoshi's Island + Mario Brothers) | `A3AJ` | 109% | 173,660 |
| 인어공주 멜로디 피치피치피치 (Mermaid Melody - Pichi Pichi Pitch) | `BMAJ` | 103% | 164,366 |

프레임스킵이 PPU 몫(~0.7M 명령어)을 비워주면 CPU 예산이 160k → 약 185k로 오릅니다.
**이 넷은 그걸로 다 들어갑니다.** 프레임스킵은 CPU 부하 자체를 줄이지는 않습니다 —
게임 로직은 매 프레임 돌아갑니다.

## 판정 불가 97개 — 다음 세션에서 할 일

루프를 못 찾으면 대기 시간이 작업량으로 계산돼서 exec가 프레임 만땅(280,896)으로 찍힙니다.
**"무겁다"가 아니라 "모른다"입니다.** 증거: 별의 커비 미국판 45% vs 일본판 176% — 같은 게임.

추적 방법 (`scripts/idlefind/`):
1. 입력을 다양화해서 더 오래 돌린다 (봇이 START/A만 눌러 메뉴를 못 넘긴 경우가 많다)
2. 다른 리전의 검증된 주소 근처(±2KB)를 정적 스캔 → 후보마다 A/B
3. 그래도 없으면 저ROM 영역 전체에서 spin 모양 브랜치를 뽑아 A/B

**A/B 없이는 절대 채택하지 말 것.** 이미 22개가 그럴듯해 보였지만 아무 일도 안 했습니다.

### 애초에 게임이 아닌 것들 (영영 안 나옴)

- `tvap` GBA TV 튜너
- `PASS` GBA 무비 플레이어 V2
- `GBAP` GBA 미니캠
- `AGBJ` 닌텐도 클라이언트 바이너리
- `AGSA` 플레이얀 AV 플레이어
