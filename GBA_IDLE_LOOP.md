# GBA — 실측 현황 (임시 메모, 지워도 됨)

전부 **게임을 실행해서 잰 것**입니다. 추정치나 DB 병합은 하나도 없습니다.

정본은 `scripts/gba_idle_loop_db.json`, 펌웨어용은 `firmware/gba/gba_idle_loop.c`,
방법론은 `scripts/idlefind/README.md`, 실기 인수인계는 `docs/GBA_FIRMWARE_HANDOFF.md`.

## 현황

| | |
|---|---|
| 측정된 게임코드 | **515** (업로드하면 즉시 매칭) |
| ├ 아이들 주소 확보 (A/B 통과) | **121** |
| ├ 루프 불필요 — BIOS halt로 이미 쉼 | 340 |
| ├ **사냥했지만 대기 루프가 없음 = 진짜 무거움** | **48** |
| └ 롬이 부팅을 안 함 (측정 불가) | 6 |
| 그중 주소를 줬는데도 예산 초과 | **6** |

이전 판의 "판정 불가 97"은 **전부 판정됐습니다**: 32개는 주소를 찾았고, 11개는 원래
가벼웠고, 48개는 진짜로 무겁고, 6개는 부팅을 안 합니다.

## 아이들 스킵이 버는 것

스킵이 없으면 스핀이 프레임을 통째로 채웁니다 — 280,896 사이클, 예산의 176%,
60fps 중 약 34fps. 주소를 주면 중앙값 76,000 사이클(예산의 48%)로 떨어집니다.

> **주소가 실린 121개 중 115개가 예산 안으로 들어옵니다. 스킵이 없으면 0개입니다.**

## 주소를 줬는데도 예산 초과 (6)

| 게임 | 코드 | 부하 | 사이클 |
|---|---|---|---|
| 얼티밋 윈터 게임즈 | `BUWE` | 127% | 203,757 |
| 슈퍼 동키콩3 | `BDQJ` | 112% | 178,881 |
| 동키콩 컨트리 3 | `BDQE` | 112% | 178,575 |
| 슈퍼마리오 요시아일랜드 | `A3AJ` | 109% | 173,660 |
| 슈퍼 차이니즈 1+2 어드밴스 | `BSAJ` | 107% | 171,663 |
| 인어공주 멜로디 피치피치피치 | `BMAJ` | 103% | 164,366 |

## 진짜 무거운 48개 — 스킵할 대기 루프가 아예 없음

찾다 만 게 아니라, **없다는 걸 확인한 것**입니다. 프레임의 사이클이 어디로 가는지 다
보고(히스토그램) 가장 뜨거운 주소들을 하나씩 강제로 스킵해 봤는데, 스킵하면 게임이
얼어붙거나 다른 화면으로 갑니다. 기다리는 게 아니라 일하고 있다는 뜻입니다.

가장 선명한 부류가 **에뮬레이터 카트**입니다 — 클래식 NES / 패미콤 미니 / 허드슨
컬렉션. 카트 안에 6502 인터프리터가 들어 있고, 뜨거운 코드는 IWRAM의 점프테이블
디스패치(`ldr pc, [ip, r1, lsl #2]`)입니다. 프레임 내내 NES를 흉내내느라 바쁩니다.
스킵할 대기가 없습니다. (이 기기에선 GBA로 NES를 돌리느니 NES 코어로 돌리는 게 낫습니다.)

## 부팅을 안 하는 6개

`A7KJ` 별의 커비 꿈의 샘 (일본판 — 2,700프레임 동안 화면이 단 한 장) ·
`tvap` TV 튜너 · `PASS` 무비 플레이어 · `GBAP` 미니캠 ·
`AGBJ` 닌텐도 클라이언트 바이너리 · `AGSA` 플레이얀 AV 플레이어.
뒤의 다섯은 주변기기가 있어야 부팅하는 물건이라 영영 안 나옵니다.

## ⚠ 예산 160,000은 추정치이고, 실기 계측은 더 빡빡합니다

배지의 100%는 M7이 프레임당 흉내낼 수 있다고 **우리가 추정한** CPU 몫입니다.
실기 계측(`gnw-gba` 커밋 `1eec5c72`, `f08cfd6c`)은 이렇게 말합니다:

| | Emulate | Draw | 프레임 |
|---|---|---|---|
| `1eec5c72` | 12.3 ms | 4.1 ms | 16.67 ms |
| `f08cfd6c` | 7.58 ms | 9.08 ms | 16.67 ms |

루비의 측정 exec가 73,266 사이클이니 사이클당 약 103~168ns입니다. Draw가 남긴 시간으로
역산하면 진짜 CPU 예산은 **약 73k~121k** — 160k가 아닙니다. 게다가 Draw는 사용자의
스케일링/필터 설정에 따라 움직입니다. **지금 배지는 1.3~2.2배 낙관적입니다.**
숫자를 바꾸려면 실기에서 한 게임을 기준으로 고정해서 재야 합니다.
